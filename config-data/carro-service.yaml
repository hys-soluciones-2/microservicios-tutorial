server:
  port: 8002
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
  instance:
    hostname: localhost

##spring:
  ##datasource:
    ##url: jdbc:mysql://localhost:3306/carro_microservicio
    ##username: root
    ##password: admin
    ##driver-class-name: com.mysql.cj.jdbc.Driver
  ##jpa:
    ##hibernate:
      ##ddl-auto: update
    ##show-sql: true
   ## properties:
      ##hibernate:
        ##format_sql: true